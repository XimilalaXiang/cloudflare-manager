# 账户管理

支持在单一界面管理多个 Cloudflare 账户。

## 添加账户
进入 **账户** → **添加账户**，填写名称、账户 ID 和 API 令牌。令牌会验证后加密存储。

## 编辑账户
点击 **编辑** 修改名称、邮箱或令牌。令牌字段留空则保持不变。

## 验证账户
点击 **验证** 检查存储的 API 令牌是否仍然有效。

## 安全性
- API 令牌使用 AES-256-GCM 加密存储
- API 响应中只显示掩码版本（`cfut****xxxx`）
- 各账户资源完全隔离
