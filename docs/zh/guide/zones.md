# 域名与 DNS

管理各账户下的域名和 DNS 记录。

## 域名浏览
选择账户后显示所有域名及其状态。

## DNS 记录管理
- **查看** — 点击域名查看 DNS 记录表格
- **创建** — 选择类型，填写名称、内容、TTL，设置代理
- **编辑** — 修改现有记录
- **删除** — 确认后删除

::: tip
TTL 设为 `1` 表示自动（使用 Cloudflare 代理时推荐）。
:::
